---
import { Image } from 'astro:assets';
import kf13Logo from '@assets/images/kf13-logo.png';

const currentYear = new Date().getFullYear();

const footerSections = [
  {
    title: "Tentang KF13",
    links: [
      { label: "Sejarah", href: "/about#sejarah" },
      { label: "Visi & Misi", href: "/about#visi-misi" },
      { label: "Tim Kami", href: "/about#tim" },
      { label: "Nilai-Nilai", href: "/about#nilai" }
    ]
  },
  {
    title: "Program",
    links: [
      { label: "Workshop", href: "/events#workshop" },
      { label: "Seminar", href: "/events#seminar" },
      { label: "Kompetisi", href: "/events#kompetisi" },
      { label: "Mentoring", href: "/events#mentoring" }
    ]
  },
  {
    title: "Konten",
    links: [
      { label: "Artikel", href: "/blog" },
      { label: "Tutorial", href: "/blog?category=tutorial" },
      { label: "Eksperimen", href: "/blog?category=eksperimen" },
      { label: "Teori", href: "/blog?category=teori" }
    ]
  },
  {
    title: "Komunitas",
    links: [
      { label: "Events", href: "/events" },
      { label: "Partnership", href: "/partnership" },
      { label: "Research", href: "/research" },
      { label: "Kontak", href: "/contact" }
    ]
  }
];
---

<footer class="bg-dark text-light border-t-4 border-primary" role="contentinfo">
  <div class="container mx-auto px-4 py-8">
    <!-- Main Footer Content -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-6 gap-6 mb-6">
      <!-- Brand Section -->
      <div class="lg:col-span-2">
        <div class="flex items-center space-x-3 mb-4">
          <div class="w-12 h-12 bg-white rounded-lg border-2 border-light/20 p-0.5 flex items-center justify-center">
            <Image 
              src={kf13Logo} 
              alt="KF13 Logo" 
              width={32} 
              height={32}
              class="w-full h-full object-contain"
            />
          </div>
          <div>
            <h3 class="text-xl font-black text-light">KF13</h3>
            <p class="text-sm text-light/70 font-semibold">Klub Fisika</p>
          </div>
        </div>
        <p class="text-light/80 mb-3 leading-relaxed text-sm">
          Komunitas fisika yang berdedikasi untuk mengembangkan pemahaman dan kecintaan terhadap ilmu fisika melalui pembelajaran, eksperimen, dan kolaborasi.
        </p>
      </div>

      <!-- Navigation Sections -->
      {footerSections.map((section) => (
        <div>
          <h4 class="font-black text-amber-400 mb-3">{section.title}</h4>
          <ul class="space-y-1">
            {section.links.map((link) => (
              <li>
                <a 
                  href={link.href}
                  class="text-light/80 hover:text-primary transition-colors font-semibold text-sm"
                >
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>

    <!-- Bottom Section -->
    <div class="border-t border-light/20 pt-4 text-center">
      <p class="text-light/60 text-sm">
        © {currentYear} KF13 - Klub Fisika. Made with 🥼🔬🧬🧫🧪 for Indonesia
      </p>
    </div>
  </div>
</footer>
