---
import Button from '../ui/Button.astro';
---

<section class="py-20 bg-gradient-to-br from-accent via-primary to-secondary dark:from-slate-700 dark:via-slate-600 dark:to-slate-500 relative overflow-hidden" id="gabung">
  <!-- Background Elements -->
  <div class="absolute inset-0 opacity-20">
    <div class="absolute top-10 left-10 w-20 h-20 bg-dark dark:bg-light transform rotate-45"></div>
    <div class="absolute bottom-10 right-10 w-16 h-16 bg-dark dark:bg-light transform -rotate-12"></div>
    <div class="absolute top-1/2 left-1/4 w-12 h-12 bg-dark dark:bg-light transform rotate-45"></div>
  </div>

  <div class="max-w-4xl mx-auto px-6 text-center relative z-10">
    <!-- Main CTA Headline -->
    <div class="bg-white dark:bg-slate-700 border-4 border-dark dark:border-light p-8 transform -rotate-1 shadow-brutal mb-8">
      <h2 class="text-4xl md:text-5xl font-black uppercase text-dark dark:text-light mb-4">
        MULAI PERJALANAN<br/>
        <span class="text-primary dark:text-primary-dark">BELAJARMU</span>
      </h2>
      <p class="text-xl font-semibold text-dark dark:text-light">
        Jadilah bagian dari komunitas fisika yang bertumbuh dan berdampak
      </p>
    </div>

    <!-- Benefits Grid -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
      {[
        {
          icon: "🎓",
          title: "BELAJAR GRATIS",
          desc: "Tidak ada biaya untuk bergabung",
          bg: "bg-pink"
        },
        {
          icon: "👥",
          title: "KOMUNITAS AKTIF",
          desc: "Bertemu teman dengan minat sama",
          bg: "bg-blue"
        },
        {
          icon: "🚀",
          title: "PENGEMBANGAN DIRI",
          desc: "Tingkatkan skill dan pengetahuan",
          bg: "bg-secondary"
        }
      ].map((benefit, index) => (
        <div class={`${benefit.bg} dark:bg-slate-600 border-4 border-dark dark:border-light p-6 transform ${index % 2 === 0 ? 'rotate-2' : '-rotate-2'} hover:rotate-0 transition-transform shadow-brutal-sm`}>
          <div class="text-3xl mb-3">{benefit.icon}</div>
          <h3 class="font-black text-dark dark:text-light mb-2">{benefit.title}</h3>
          <p class="text-sm font-semibold text-dark dark:text-light">{benefit.desc}</p>
        </div>
      ))}
    </div>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
      <Button 
        variant="primary" 
        size="lg" 
        href="http://47.84.35.77/wp/portal/?fcom_action=auth"
        className="bounce-fun text-xl px-12 py-6"
      >
        🚀 DAFTAR SEKARANG
      </Button>
      
      <Button 
        variant="secondary" 
        size="lg" 
        href="#kontak"
        className="transform rotate-1 hover:rotate-0"
      >
        💬 HUBUNGI KAMI
      </Button>
    </div>

    <!-- Additional Info -->
    <div class="mt-8">
      <div class="bg-surface dark:bg-slate-600 border-4 border-dark dark:border-light p-4 transform rotate-1 inline-block shadow-brutal-sm">
        <p class="text-sm font-bold text-dark dark:text-light">
          ✨ Platform Beta • Web 3.0 Ready • Made with 🥼🔬🧬 for Indonesia
        </p>
      </div>
    </div>
  </div>
</section>
