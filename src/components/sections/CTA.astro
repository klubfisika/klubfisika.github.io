---
import BrutalButton from '../ui/BrutalButton.astro';
import BrutalCard from '../ui/BrutalCard.astro';
import Heading from '../ui/Heading.astro';
import Text from '../ui/Text.astro';
import GradientSection from '../ui/GradientSection.astro';
---

<GradientSection variant="accent-primary" class="relative overflow-hidden" id="gabung">
  <!-- Background Elements -->
  <div class="absolute inset-0 opacity-20">
    <div class="absolute top-10 left-10 w-20 h-20 bg-dark dark:bg-light transform rotate-45"></div>
    <div class="absolute bottom-10 right-10 w-16 h-16 bg-dark dark:bg-light transform -rotate-12"></div>
    <div class="absolute top-1/2 left-1/4 w-12 h-12 bg-dark dark:bg-light transform rotate-45"></div>
  </div>

  <div class="max-w-4xl mx-auto px-6 text-center relative z-10">
    <!-- Main CTA Headline -->
    <BrutalCard variant="white" rotation="left" className="p-8 mb-4">
      <Heading level={2} size="3xl" class="uppercase mb-4">
        MULAI PERJALANAN<br/>
        <span class="text-primary dark:text-primary-dark">BELAJARMU</span>
      </Heading>
      <Text size="lg" class="font-semibold">
        Jadilah bagian dari komunitas fisika yang bertumbuh dan berdampak
      </Text>
    </BrutalCard>

    <!-- Benefits Grid -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-4">
      {[
        {
          icon: "🎓",
          title: "BELAJAR GRATIS",
          desc: "Tidak ada biaya untuk bergabung",
          variant: "primary"
        },
        {
          icon: "👥",
          title: "KOMUNITAS AKTIF",
          desc: "Bertemu teman dengan minat sama",
          variant: "secondary"
        },
        {
          icon: "🚀",
          title: "PENGEMBANGAN DIRI",
          desc: "Tingkatkan skill dan pengetahuan",
          variant: "accent"
        }
      ].map((benefit, index) => (
        <BrutalCard variant={benefit.variant} rotation={index % 2 === 0 ? 'right' : 'left'} className="p-6">
          <div class="text-3xl mb-3">{benefit.icon}</div>
          <Heading level={3} size="sm" color="white" class="mb-2">{benefit.title}</Heading>
          <Text size="sm" color="white" class="font-semibold">{benefit.desc}</Text>
        </BrutalCard>
      ))}
    </div>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
      <BrutalButton 
        variant="primary" 
        size="lg" 
        href="http://47.84.35.77/wp/portal/?fcom_action=auth"
        class="bounce-fun text-xl px-12 py-6"
        target="_blank"
        rel="noopener noreferrer"
        ariaLabel="Daftar sekarang ke portal KF13"
      >
        🚀 DAFTAR SEKARANG
      </BrutalButton>
      
      <BrutalButton 
        variant="secondary" 
        size="lg" 
        href="#kontak"
        class="transform rotate-1 hover:rotate-0"
        ariaLabel="Hubungi kami"
      >
        💬 HUBUNGI KAMI
      </BrutalButton>
    </div>

    <!-- Additional Info -->
    <div class="mt-8">
      <BrutalCard variant="surface" rotation="right" className="inline-block">
        <Text size="sm" class="font-bold">
          ✨ Platform Beta • Web 3.0 Ready • Made with 🥼🔬🧬 for Indonesia
        </Text>
      </BrutalCard>
    </div>
  </div>
</GradientSection>
