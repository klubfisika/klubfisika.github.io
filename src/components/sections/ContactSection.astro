---
import SectionHeader from '../ui/SectionHeader.astro';
import ContactForm from '../ui/ContactForm.astro';
import BrutalCard from '../ui/BrutalCard.astro';
import BrutalButton from '../ui/BrutalButton.astro';
import Heading from '../ui/Heading.astro';
import Text from '../ui/Text.astro';
import BrutalSection from '../ui/BrutalSection.astro';
---

<BrutalSection background="surface" padding="lg" id="kontak">
  <div class="max-w-6xl mx-auto px-6">
    <SectionHeader title="KONTAK & MEDIA SOSIAL" className="mb-8" />
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-12">
      <!-- Contact Form -->
      <div>
        <ContactForm />
      </div>

      <!-- Contact Info & Social Media -->
      <div class="space-y-8">
        <BrutalCard variant="primary" rotation="left" className="p-8">
          <Heading level={3} size="lg" color="on-primary" class="uppercase mb-6">INFO KONTAK</Heading>
          
          <div class="space-y-4">
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 bg-secondary border-2 border-border-main flex items-center justify-center">
                <span class="text-xl">📧</span>
              </div>
              <div>
                <Text color="on-primary" class="font-bold">Email Resmi</Text>
                <Text color="on-primary">info@klubfisika.org</Text>
              </div>
            </div>
            
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 bg-accent border-2 border-border-main flex items-center justify-center">
                <span class="text-xl">💬</span>
              </div>
              <div>
                <Text color="on-primary" class="font-bold">WhatsApp Admin</Text>
                <Text color="on-primary">+62 822-2500-9253</Text>
              </div>
            </div>
            
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 bg-secondary border-2 border-border-main flex items-center justify-center">
                <span class="text-xl">🌐</span>
              </div>
              <div>
                <Text color="on-primary" class="font-bold">Website</Text>
                <Text color="on-primary">klubfisika.github.io</Text>
              </div>
            </div>
          </div>
        </BrutalCard>

        <!-- Social Media Grid -->
        <BrutalCard variant="accent" rotation="right" className="p-8">
          <Heading level={3} size="lg" color="on-accent" class="uppercase mb-6">MEDIA SOSIAL</Heading>
          
          <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
            {[
              { name: "Instagram", icon: "📸", handle: "@kf13_indonesia", variant: "secondary", url: "https://instagram.com/kf13_indonesia" },
              { name: "YouTube", icon: "📺", handle: "KF13 Indonesia", variant: "primary", url: "https://youtube.com/@KF13Indonesia" },
              { name: "Telegram", icon: "✈️", handle: "@kf13indonesia", variant: "accent", url: "https://t.me/kf13indonesia" },
              { name: "Discord", icon: "🎮", handle: "KF13 Community", variant: "primary", url: "https://discord.gg/kf13indonesia" },
              { name: "GitHub", icon: "💻", handle: "klubfisika", variant: "dark", url: "https://github.com/klubfisika" },
              { name: "Email", icon: "📧", handle: "info@klubfisika.org", variant: "accent", url: "mailto:info@klubfisika.org" }
            ].map((social, index) => (
              <BrutalButton
                variant={social.variant as any}
                href={social.url}
                target={social.name === "Email" ? "_self" : "_blank"}
                rel={social.name === "Email" ? "" : "noopener noreferrer"}
                className={`p-4 text-center transform ${index % 2 === 0 ? 'rotate-2' : '-rotate-2'} hover:rotate-0`}
                ariaLabel={`${social.name} - ${social.handle}`}
              >
                <div class="text-2xl mb-2">{social.icon}</div>
                <p class="font-bold text-xs">{social.name}</p>
                <p class="text-xs opacity-90">{social.handle}</p>
              </BrutalButton>
            ))}
          </div>
        </BrutalCard>
      </div>
    </div>
  </div>
</BrutalSection>
