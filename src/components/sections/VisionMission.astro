---
import SectionHeader from '../ui/SectionHeader.astro';
import BrutalCard from '../ui/BrutalCard.astro';
import Heading from '../ui/Heading.astro';
import Text from '../ui/Text.astro';
import BrutalSection from '../ui/BrutalSection.astro';
import OrbitalElectron from '../ui/OrbitalElectron.astro';
---

<BrutalSection background="white" padding="lg">
  <div class="max-w-6xl mx-auto px-6">
    <SectionHeader title="VISI & MISI" subtitle="Arah dan prinsip KF13" className="mb-8" />
    
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
      <!-- Visi -->
      <div class="space-y-8">
        <BrutalCard variant="primary" rotation="left" className="text-center p-8">
          <div class="text-5xl mb-4">🎯</div>
          <Heading level={3} size="lg" color="white" class="uppercase mb-4">VISI</Heading>
          <Text size="lg" color="white" class="font-semibold leading-relaxed">
            Menjadi komunitas fisika yang berkontribusi nyata dalam pengembangan generasi muda yang kritis, ilmiah, dan inovatif.
          </Text>
        </BrutalCard>
      </div>

      <!-- Misi -->
      <div class="space-y-6">
        <BrutalCard variant="secondary" rotation="right" className="text-center">
          <Heading level={3} size="lg" color="white" class="uppercase">MISI</Heading>
        </BrutalCard>
        
        <div class="space-y-4">
          {[
            {
              icon: "📚",
              text: "Menyediakan ruang pembelajaran fisika yang aktif dan kolaboratif",
              variant: "primary",
              rotation: "left"
            },
            {
              icon: "🔬",
              text: "Mendorong eksplorasi, eksperimen, dan proyek fisika terapan",
              variant: "accent",
              rotation: "right"
            },
            {
              icon: "🧠",
              text: "Menumbuhkan budaya berpikir ilmiah dan rasa ingin tahu",
              variant: "secondary",
              rotation: "left"
            },
            {
              icon: "🌐",
              text: "Membuka akses jejaring sains yang lebih luas",
              variant: "primary",
              rotation: "right"
            }
          ].map((misi, index) => (
            <BrutalCard variant={misi.variant} rotation={misi.rotation} className="p-4">
              <div class="flex items-center gap-4">
                <div class="text-2xl">{misi.icon}</div>
                <Text color="white" class="font-semibold">{misi.text}</Text>
              </div>
            </BrutalCard>
          ))}
        </div>
      </div>
    </div>

    <!-- Nilai Inti -->
    <div class="mt-8">
      <div class="text-center mb-8">
        <BrutalCard variant="accent" rotation="right" className="inline-block px-8 py-4">
          <Heading level={3} size="lg" color="white" class="uppercase m-0">NILAI INTI</Heading>
        </BrutalCard>
      </div>
      
      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-6">
        <BrutalCard variant="primary" rotation="right" className="text-center p-6">
          <div class="text-4xl mb-3">🔬</div>
          <Text color="white" size="sm" class="font-black">ILMIAH</Text>
        </BrutalCard>
        
        <BrutalCard variant="secondary" rotation="left" className="text-center p-6">
          <div class="text-4xl mb-3">🚀</div>
          <Text color="white" size="sm" class="font-black">EKSPLORATIF</Text>
        </BrutalCard>
        
        <BrutalCard variant="accent" rotation="right" className="text-center p-6">
          <div class="text-4xl mb-3">🤝</div>
          <Text color="white" size="sm" class="font-black">KOLABORATIF</Text>
        </BrutalCard>
        
        <BrutalCard variant="primary" rotation="left" className="text-center p-6">
          <div class="text-4xl mb-3">🌈</div>
          <Text color="white" size="sm" class="font-black">INKLUSIF</Text>
        </BrutalCard>
        
        <BrutalCard variant="secondary" rotation="right" className="text-center p-6">
          <div class="text-4xl mb-3">⚖️</div>
          <Text color="white" size="sm" class="font-black">BERINTEGRITAS</Text>
        </BrutalCard>
      </div>
    </div>
  </div>
</BrutalSection>
