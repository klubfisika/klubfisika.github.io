---
import BrutalButton from '../ui/BrutalButton.astro';
import BrutalCard from '../ui/BrutalCard.astro';
import Heading from '../ui/Heading.astro';
import Text from '../ui/Text.astro';
import GradientSection from '../ui/GradientSection.astro';
---

<GradientSection variant="purple-green" class="min-h-screen flex items-center relative overflow-hidden">
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-5 dark:opacity-10">
    <div class="absolute top-20 left-20 w-16 h-16 bg-dark dark:bg-light transform rotate-45"></div>
    <div class="absolute top-40 right-32 w-12 h-12 bg-dark dark:bg-light transform rotate-12"></div>
    <div class="absolute bottom-32 left-1/3 w-10 h-10 bg-dark dark:bg-light transform -rotate-12"></div>
  </div>

  <div class="max-w-6xl mx-auto px-6 relative z-10">
    <!-- Main Content -->
    <div class="text-center mb-8">
      <Heading level={1} size="5xl" class="uppercase tracking-tight transform -rotate-1 mb-6">
        RUANG BERTUMBUH<br/>
        <span class="text-primary dark:text-primary-dark">FISIKA</span> INDONESIA
      </Heading>
      
      <Text size="xl" class="font-bold mb-4">
        Belajar • Eksperimen • Kolaborasi • Fun! 🧪⚡
      </Text>
      
      <Text size="lg" class="max-w-3xl mx-auto leading-relaxed mb-8">
        KF13 adalah komunitas pembelajaran dan eksplorasi fisika yang mendorong generasi muda untuk memahami sains secara mendalam, aplikatif, dan kolaboratif.
      </Text>
    </div>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center mb-12">
      <BrutalButton variant="primary" size="lg" href="/mulai" className="bounce-fun">
        🚀 GABUNG KF13
      </BrutalButton>
      <BrutalButton variant="secondary" size="lg" href="#program" className="transform rotate-1 hover:rotate-0">
        📚 LIHAT PROGRAM
      </BrutalButton>
    </div>

    <!-- Floating Stats -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
      <BrutalCard variant="primary" rotation="left" className="text-center p-6">
        <div class="text-4xl font-black text-white mb-2">500+</div>
        <Text color="white" size="sm" class="font-bold">Anggota Aktif</Text>
      </BrutalCard>
      
      <BrutalCard variant="secondary" rotation="right" className="text-center p-6">
        <div class="text-4xl font-black text-white mb-2">100+</div>
        <Text color="white" size="sm" class="font-bold">Eksperimen</Text>
      </BrutalCard>
      
      <BrutalCard variant="accent" rotation="left" className="text-center p-6">
        <div class="text-4xl font-black text-white mb-2">50+</div>
        <Text color="white" size="sm" class="font-bold">Event</Text>
      </BrutalCard>
    </div>
  </div>
</GradientSection>
