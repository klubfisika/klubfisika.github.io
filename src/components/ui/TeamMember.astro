---
interface Props {
  name: string;
  role: string;
  bio?: string;
  avatar?: string;
  social?: {
    instagram?: string;
    linkedin?: string;
    github?: string;
  };
  variant?: 'primary' | 'secondary' | 'accent';
  className?: string;
}

const { 
  name,
  role,
  bio,
  avatar,
  social,
  variant = 'primary',
  className = ""
} = Astro.props;
---

<HoverScale>
  <BrutalCard variant={variant} rotation="left" className={`text-center ${className}`}>
    <div class="mb-4">
      {avatar ? (
        <div class="w-20 h-20 bg-white border-2 border-dark dark:border-light rounded-full mx-auto mb-3 overflow-hidden">
          <img src={avatar} alt={name} class="w-full h-full object-cover" />
        </div>
      ) : (
        <div class="w-20 h-20 bg-white border-2 border-dark dark:border-light rounded-full mx-auto mb-3 flex items-center justify-center">
          <span class="text-3xl">👨‍🔬</span>
        </div>
      )}
      
      <h3 class="text-lg font-black uppercase text-on-primary mb-1">{name}</h3>
      <p class="text-sm font-bold text-on-primary opacity-90">{role}</p>
    </div>
    
    {bio && (
      <p class="text-xs text-on-primary opacity-80 leading-relaxed mb-4">
        {bio}
      </p>
    )}
    
    {social && (
      <FlexRow justify="center" gap="2">
        {social.instagram && (
          <a href={social.instagram} target="_blank" rel="noopener noreferrer" class="text-on-primary hover:scale-110 transition-transform">
            📸
          </a>
        )}
        {social.linkedin && (
          <a href={social.linkedin} target="_blank" rel="noopener noreferrer" class="text-on-primary hover:scale-110 transition-transform">
            💼
          </a>
        )}
        {social.github && (
          <a href={social.github} target="_blank" rel="noopener noreferrer" class="text-on-primary hover:scale-110 transition-transform">
            💻
          </a>
        )}
      </FlexRow>
    )}
  </BrutalCard>
</HoverScale>
