---
interface Props {
  variant?: 'light' | 'dark' | 'primary' | 'secondary' | 'accent';
  size?: 'xs' | 'sm' | 'md';
  className?: string;
}

const { 
  variant = 'light',
  size = 'sm',
  className = ""
} = Astro.props;

const variantClasses = {
  light: 'bg-surface text-text-main border-border-main',
  dark: 'bg-text-main text-bg border-border-main',
  primary: 'btn-primary text-xs',
  secondary: 'btn-secondary text-xs', 
  accent: 'btn-accent text-xs'
};

const sizeClasses = {
  xs: 'px-2 py-1 text-xs',
  sm: 'px-3 py-1 text-xs',
  md: 'px-4 py-2 text-sm'
};

const badgeClasses = `${variantClasses[variant]} ${sizeClasses[size]} font-bold border-2 inline-block ${className}`;
---

<span class={badgeClasses}>
  <slot />
</span>
