---
interface Props {
  icon?: string;
  title: string;
  description: string;
  schedule?: string;
  level?: string;
  participants?: string;
  bgColor?: string;
  rotation?: string;
  className?: string;
}

const { 
  icon = "🔬", 
  title, 
  description,
  schedule,
  level,
  participants,
  bgColor = "bg-pink", 
  rotation = "-rotate-2",
  className = ""
} = Astro.props;
---

<div class={`${bgColor} card-brutal p-6 transform ${rotation} hover:rotate-0 ${className}`}>
  <div class="text-4xl mb-4">{icon}</div>
  <h3 class="text-2xl font-black uppercase text-dark mb-3">{title}</h3>
  <p class="text-dark font-semibold mb-4">{description}</p>
  
  {(schedule || level || participants) && (
    <div class="border-t-2 border-dark pt-3 mt-3 space-y-2">
      {schedule && (
        <div class="text-xs font-bold text-dark">
          📅 {schedule}
        </div>
      )}
      {level && (
        <div class="text-xs font-bold text-dark">
          📊 Level: {level}
        </div>
      )}
      {participants && (
        <div class="text-xs font-bold text-dark">
          👥 {participants}
        </div>
      )}
    </div>
  )}
</div>
