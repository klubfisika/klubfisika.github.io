---
import BrutalCard from './BrutalCard.astro';
import Heading from './Heading.astro';
import Text from './Text.astro';

interface Props {
  icon?: string;
  title: string;
  description: string;
  schedule?: string;
  level?: string;
  participants?: string;
  variant?: 'primary' | 'secondary' | 'accent' | 'surface' | 'dark' | 'white';
  rotation?: 'left' | 'right' | 'none';
  className?: string;
}

const { 
  icon = "🔬", 
  title, 
  description,
  schedule,
  level,
  participants,
  variant = "primary", 
  rotation = "left",
  className = ""
} = Astro.props;
---

<BrutalCard variant={variant} rotation={rotation} class={`p-6 ${className}`}>
  <div class="text-4xl mb-4">{icon}</div>
  <Heading level={3} size="lg" color="white" class="uppercase mb-3">{title}</Heading>
  <Text color="white" class="font-semibold mb-4">{description}</Text>
  
  {(schedule || level || participants) && (
    <div class="border-t-2 border-white pt-3 mt-3 space-y-2">
      {schedule && (
        <Text color="white" size="xs" class="font-bold">
          📅 {schedule}
        </Text>
      )}
      {level && (
        <Text color="white" size="xs" class="font-bold">
          📊 Level: {level}
        </Text>
      )}
      {participants && (
        <Text color="white" size="xs" class="font-bold">
          👥 {participants}
        </Text>
      )}
    </div>
  )}
</BrutalCard>
