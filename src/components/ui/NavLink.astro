---
interface Props {
  href: string;
  active?: boolean;
  external?: boolean;
  className?: string;
}

const { 
  href,
  active = false,
  external = false,
  className = ""
} = Astro.props;

const linkClasses = `font-bold text-text-main hover:text-primary transition-colors transform hover:scale-105 ${active ? 'text-primary' : ''} ${className}`;
---

<a 
  href={href}
  target={external ? '_blank' : undefined}
  rel={external ? 'noopener noreferrer' : undefined}
  class={linkClasses}
>
  <slot />
</a>
