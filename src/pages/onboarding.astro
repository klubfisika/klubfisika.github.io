---
import AuthLayout from '../layouts/AuthLayout.astro';
---

<AuthLayout 
  title="Setup Profil" 
  description="Lengkapi profil untuk pengalaman yang lebih personal"
  bgClass="bg-gradient-to-br from-green-50 to-teal-50"
>
  <div class="min-h-screen flex items-center justify-center p-4">
      <div class="w-full max-w-lg">
        <!-- Progress -->
        <div class="flex justify-center mb-8">
          <div class="flex items-center gap-2">
            <div class="w-8 h-8 bg-green-600 rounded-full flex items-center justify-center text-white text-sm font-bold">1</div>
            <div class="w-12 h-1 bg-green-600 rounded"></div>
            <div id="step2" class="w-8 h-8 bg-gray-200 rounded-full flex items-center justify-center text-gray-500 text-sm font-bold">2</div>
            <div class="w-12 h-1 bg-gray-200 rounded"></div>
            <div id="step3" class="w-8 h-8 bg-gray-200 rounded-full flex items-center justify-center text-gray-500 text-sm font-bold">3</div>
            <div class="w-12 h-1 bg-gray-200 rounded"></div>
            <div id="step4" class="w-8 h-8 bg-gray-200 rounded-full flex items-center justify-center text-gray-500 text-sm font-bold">4</div>
          </div>
        </div>

        <!-- Card -->
        <div class="bg-white rounded-3xl shadow-xl p-8 animate-in fade-in duration-300">
          <!-- Step 1: Level -->
          <div id="form-step1" class="animate-in slide-in-from-right duration-300">
            <h2 class="text-2xl font-bold text-center mb-2">Kamu sekarang di mana? 🎓</h2>
            <p class="text-gray-500 text-center mb-6">Pilih yang paling sesuai</p>
            
            <!-- Show user info from /mulai -->
            <div class="bg-green-50 border border-green-200 rounded-xl p-4 mb-6">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 bg-green-600 rounded-full flex items-center justify-center text-white font-bold">
                  <span id="user-initial">?</span>
                </div>
                <div>
                  <div class="font-semibold" id="user-name">Loading...</div>
                  <div class="text-sm text-gray-600" id="user-phone">Loading...</div>
                </div>
              </div>
            </div>
            
            <div class="space-y-3">
              <button class="level-btn w-full p-4 border-2 border-gray-200 rounded-xl hover:border-green-500 hover:bg-green-50 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98] text-left" data-level="SMP">
                <div class="flex items-center gap-3">
                  <span class="text-2xl">📚</span>
                  <div>
                    <div class="font-semibold">SMP</div>
                    <div class="text-sm text-gray-500">Baru mulai belajar fisika</div>
                  </div>
                </div>
              </button>
              <button class="level-btn w-full p-4 border-2 border-gray-200 rounded-xl hover:border-green-500 hover:bg-green-50 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98] text-left" data-level="SMA">
                <div class="flex items-center gap-3">
                  <span class="text-2xl">🏆</span>
                  <div>
                    <div class="font-semibold">SMA</div>
                    <div class="text-sm text-gray-500">Persiapan OSN, eksperimen</div>
                  </div>
                </div>
              </button>
              <button class="level-btn w-full p-4 border-2 border-gray-200 rounded-xl hover:border-green-500 hover:bg-green-50 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98] text-left" data-level="Universitas">
                <div class="flex items-center gap-3">
                  <span class="text-2xl">🔬</span>
                  <div>
                    <div class="font-semibold">Kuliah</div>
                    <div class="text-sm text-gray-500">S1 Fisika atau jurusan terkait</div>
                  </div>
                </div>
              </button>
              <button class="level-btn w-full p-4 border-2 border-gray-200 rounded-xl hover:border-green-500 hover:bg-green-50 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98] text-left" data-level="S2/S3">
                <div class="flex items-center gap-3">
                  <span class="text-2xl">🎯</span>
                  <div>
                    <div class="font-semibold">Pascasarjana</div>
                    <div class="text-sm text-gray-500">S2, S3, atau peneliti</div>
                  </div>
                </div>
              </button>
            </div>
          </div>

          <!-- Step 2: Institution Details -->
          <div id="form-step2" class="hidden animate-in slide-in-from-right duration-300">
            <h2 class="text-2xl font-bold text-center mb-2">Detail Institusi 🏫</h2>
            <p class="text-gray-500 text-center mb-6" id="institution-subtitle">Ceritakan tentang tempat belajar kamu</p>
            
            <div class="space-y-4">
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Nama Institusi</label>
                <div class="relative">
                  <input 
                    type="text" 
                    id="institution-name" 
                    placeholder="Ketik nama sekolah/universitas..."
                    class="w-full p-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-green-500 focus:border-transparent outline-none transition-all duration-200 focus:shadow-lg focus:scale-[1.01]"
                    autocomplete="off"
                    required
                  />
                  <div id="institution-suggestions" class="absolute z-10 w-full bg-white border border-gray-200 rounded-xl shadow-lg mt-1 max-h-60 overflow-y-auto hidden">
                    <!-- Suggestions will be populated here -->
                  </div>
                </div>
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-2" id="year-label">Tahun Masuk</label>
                <select 
                  id="year-select"
                  class="w-full p-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-green-500 focus:border-transparent outline-none transition-all duration-200 focus:shadow-lg focus:scale-[1.01]"
                  onchange="checkStep2Completion()"
                  required
                >
                  <option value="">Pilih tahun</option>
                  <option value="2024">2024</option>
                  <option value="2023">2023</option>
                  <option value="2022">2022</option>
                  <option value="2021">2021</option>
                  <option value="2020">2020</option>
                  <option value="2019">2019</option>
                </select>
              </div>
              <div id="major-field" class="hidden">
                <label class="block text-sm font-medium text-gray-700 mb-2">Jurusan/Program Studi</label>
                <input 
                  type="text" 
                  id="major-input"
                  placeholder="Contoh: Fisika / Teknik Fisika / IPA"
                  class="w-full p-3 border border-gray-200 rounded-xl focus:ring-2 focus:ring-green-500 focus:border-transparent outline-none transition-all duration-200 focus:shadow-lg focus:scale-[1.01]"
                  oninput="checkStep2Completion()"
                />
              </div>
            </div>
            
            <button id="next-step2" class="w-full mt-6 bg-green-600 text-white py-3 rounded-xl font-semibold hover:bg-green-700 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98] disabled:opacity-50 disabled:hover:scale-100" disabled>
              Lanjut
            </button>
          </div>

          <!-- Step 3: Interests -->
          <div id="form-step3" class="hidden animate-in slide-in-from-right duration-300">
            <h2 class="text-2xl font-bold text-center mb-2">Apa yang menarik buatmu? ✨</h2>
            <p class="text-gray-500 text-center mb-6">Pilih beberapa yang kamu suka</p>
            
            <div class="grid grid-cols-2 gap-3">
              <button class="interest-btn p-3 border-2 border-gray-200 rounded-xl hover:border-green-500 hover:bg-green-50 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98] text-center" data-interest="Mekanika">
                <div class="text-2xl mb-1">⚙️</div>
                <div class="text-sm font-medium">Mekanika</div>
              </button>
              <button class="interest-btn p-3 border-2 border-gray-200 rounded-xl hover:border-green-500 hover:bg-green-50 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98] text-center" data-interest="Kuantum">
                <div class="text-2xl mb-1">⚛️</div>
                <div class="text-sm font-medium">Kuantum</div>
              </button>
              <button class="interest-btn p-3 border-2 border-gray-200 rounded-xl hover:border-green-500 hover:bg-green-50 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98] text-center" data-interest="Astrofisika">
                <div class="text-2xl mb-1">🌌</div>
                <div class="text-sm font-medium">Astrofisika</div>
              </button>
              <button class="interest-btn p-3 border-2 border-gray-200 rounded-xl hover:border-green-500 hover:bg-green-50 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98] text-center" data-interest="Eksperimen">
                <div class="text-2xl mb-1">🔬</div>
                <div class="text-sm font-medium">Eksperimen</div>
              </button>
              <button class="interest-btn p-3 border-2 border-gray-200 rounded-xl hover:border-green-500 hover:bg-green-50 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98] text-center" data-interest="Olimpiade">
                <div class="text-2xl mb-1">🏆</div>
                <div class="text-sm font-medium">Olimpiade</div>
              </button>
              <button class="interest-btn p-3 border-2 border-gray-200 rounded-xl hover:border-green-500 hover:bg-green-50 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98] text-center" data-interest="Komputasi">
                <div class="text-2xl mb-1">💻</div>
                <div class="text-sm font-medium">Komputasi</div>
              </button>
            </div>
            
            <button id="next-step3" class="w-full mt-6 bg-green-600 text-white py-3 rounded-xl font-semibold hover:bg-green-700 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98] disabled:opacity-50 disabled:hover:scale-100" disabled>
              Lanjut
            </button>
          </div>

          <!-- Step 4: Bio -->
          <div id="form-step4" class="hidden animate-in slide-in-from-right duration-300">
            <h2 class="text-2xl font-bold text-center mb-2">Ceritain dikit tentang kamu 😊</h2>
            <p class="text-gray-500 text-center mb-6">Biar teman-teman bisa kenal</p>
            
            <div class="space-y-4">
              <div>
                <textarea id="bio" rows="4" placeholder="Contoh: Suka eksperimen fisika, lagi persiapan OSN, pengen jadi peneliti..." 
                  class="w-full p-4 border border-gray-200 rounded-xl focus:ring-2 focus:ring-green-500 focus:border-green-500 resize-none transition-all duration-200 focus:shadow-lg focus:scale-[1.01]"></textarea>
              </div>
              
              <button id="finish" class="w-full bg-gradient-to-r from-green-600 to-teal-600 text-white py-3 rounded-xl font-semibold hover:from-green-700 hover:to-teal-700 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98]">
                Mulai Eksplorasi! 🚀
              </button>
            </div>
          </div>
        </div>

        <div class="text-center mt-6">
          <a href="#" id="back-link" class="text-sm text-gray-500 hover:text-green-600 transition-colors">← Kembali</a>
        </div>
      </div>
    </div>

    <script is:inline>
      let userData = JSON.parse(sessionStorage.getItem('kf13-onboarding') || '{}');
      let selectedLevel = '';
      let selectedInterests = [];
      let institutionsData = {};

      // Load institutions database
      fetch('/institutions-diy.json')
        .then(response => {
          if (!response.ok) {
            throw new Error(`HTTP error! status: ${response.status}`);
          }
          return response.json();
        })
        .then(data => {
          institutionsData = data;
          console.log('Institutions data loaded successfully');
        })
        .catch(error => {
          console.error('Failed to load institutions data:', error);
        });

      // Institution autocomplete
      function setupInstitutionAutocomplete() {
        const input = document.getElementById('institution-name');
        const suggestions = document.getElementById('institution-suggestions');
        
        input.addEventListener('input', (e) => {
          const query = e.target.value.toLowerCase().trim();
          
          if (query.length < 2) {
            suggestions.classList.add('hidden');
            return;
          }
          
          const matches = [];
          const currentLevel = selectedLevel.toLowerCase();
          
          // Get relevant institutions based on level
          Object.keys(institutionsData).forEach(city => {
            const cityData = institutionsData[city];
            let categories = [];
            
            if (currentLevel === 'smp') categories = ['smp'];
            else if (currentLevel === 'sma') categories = ['sma', 'smk'];
            else if (currentLevel === 'universitas' || currentLevel === 's2/s3') categories = ['universitas'];
            
            categories.forEach(category => {
              if (cityData[category]) {
                cityData[category].forEach(institution => {
                  if (institution.toLowerCase().includes(query)) {
                    matches.push(institution);
                  }
                });
              }
            });
          });
          
          // Display suggestions
          if (matches.length > 0) {
            suggestions.innerHTML = matches.slice(0, 8).map(match => 
              `<div class="p-3 hover:bg-gray-50 cursor-pointer border-b border-gray-100 last:border-b-0" onclick="selectInstitution('${match}')">${match}</div>`
            ).join('');
            suggestions.classList.remove('hidden');
          } else {
            suggestions.classList.add('hidden');
          }
        });
        
        // Hide suggestions when clicking outside
        document.addEventListener('click', (e) => {
          if (!input.contains(e.target) && !suggestions.contains(e.target)) {
            suggestions.classList.add('hidden');
          }
        });
      }
      
      function selectInstitution(institutionName) {
        document.getElementById('institution-name').value = institutionName;
        document.getElementById('institution-suggestions').classList.add('hidden');
        checkStep2Completion();
      }

      // Email validation function
      function validateEmail(email) {
        const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        return emailRegex.test(email);
      }

      // Save email to localStorage
      function saveEmail(email) {
        if (validateEmail(email)) {
          localStorage.setItem('kf13-email', email);
        }
      }

      // Load saved email
      function loadSavedEmail() {
        const savedEmail = localStorage.getItem('kf13-email');
        const emailInput = document.getElementById('email');
        if (savedEmail && emailInput) {
          emailInput.value = savedEmail;
        }
      }

      // Load and display user data from /mulai
      document.addEventListener('DOMContentLoaded', () => {
        // Display user info
        const userNameEl = document.getElementById('user-name');
        const userPhoneEl = document.getElementById('user-phone');
        const userInitialEl = document.getElementById('user-initial');
        
        if (userData.name && userData.phone) {
          userNameEl.textContent = userData.name;
          userPhoneEl.textContent = userData.phone;
          userInitialEl.textContent = userData.name.charAt(0).toUpperCase();
        } else {
          userNameEl.textContent = 'Data tidak ditemukan';
          userPhoneEl.textContent = 'Silakan mulai dari halaman login';
        }
        
        // Initialize back link
        updateBackLink();
      });

      // Step 1: Level selection
      document.querySelectorAll('.level-btn').forEach(btn => {
        btn.addEventListener('click', () => {
          // No need to validate inputs - data comes from /mulai
          if (!userData.name || !userData.phone) {
            alert('Data tidak lengkap. Silakan mulai dari halaman login.');
            return;
          }
          
          document.querySelectorAll('.level-btn').forEach(b => {
            b.classList.remove('border-green-500', 'bg-green-50');
            b.classList.add('border-gray-200');
          });
          btn.classList.add('border-green-500', 'bg-green-50');
          btn.classList.remove('border-gray-200');
          
          selectedLevel = btn.dataset.level;
          
          // Update institution step based on level
          updateInstitutionStep(selectedLevel);
          
          setTimeout(() => {
            document.getElementById('form-step1').classList.add('hidden');
            document.getElementById('form-step2').classList.remove('hidden');
            currentStepNum = 2;
            updateProgressBar(2);
            updateBackLink();
          }, 300);
        });
      });

      // Step 2: Interests selection
      document.querySelectorAll('.interest-btn').forEach(btn => {
        btn.addEventListener('click', () => {
          const interest = btn.dataset.interest;
          if (selectedInterests.includes(interest)) {
            selectedInterests = selectedInterests.filter(i => i !== interest);
            btn.classList.remove('border-green-500', 'bg-green-50');
            btn.classList.add('border-gray-200');
          } else {
            selectedInterests.push(interest);
            btn.classList.add('border-green-500', 'bg-green-50');
            btn.classList.remove('border-gray-200');
          }
          
          document.getElementById('next-step2').disabled = selectedInterests.length === 0;
        });
      });

      // Update institution step based on selected level
      function updateInstitutionStep(level) {
        const subtitle = document.getElementById('institution-subtitle');
        const yearLabel = document.getElementById('year-label');
        const majorField = document.getElementById('major-field');
        
        if (level === 'SMP' || level === 'SMA') {
          subtitle.textContent = 'Ceritakan tentang sekolah kamu';
          yearLabel.textContent = 'Tahun Masuk';
          majorField.classList.add('hidden');
        } else if (level === 'Universitas') {
          subtitle.textContent = 'Ceritakan tentang universitas kamu';
          yearLabel.textContent = 'Tahun Masuk';
          majorField.classList.remove('hidden');
        } else if (level === 'S2/S3') {
          subtitle.textContent = 'Ceritakan tentang institusi penelitian kamu';
          yearLabel.textContent = 'Tahun Mulai Program';
          majorField.classList.remove('hidden');
        }
        
        // Setup autocomplete after level is selected
        setTimeout(() => {
          setupInstitutionAutocomplete();
        }, 100);
      }

      // Check step 2 completion
      function checkStep2Completion() {
        const institutionName = document.getElementById('institution-name').value.trim();
        const yearSelect = document.getElementById('year-select').value;
        const majorInput = document.getElementById('major-input');
        
        let isComplete = institutionName && yearSelect;
        
        // Check major field if visible
        if (!majorInput.parentElement.classList.contains('hidden')) {
          isComplete = isComplete && majorInput.value.trim();
        }
        
        document.getElementById('next-step2').disabled = !isComplete;
      }

      // Progress bar helper
      function updateProgressBar(step) {
        const steps = ['step2', 'step3', 'step4'];
        const lines = document.querySelectorAll('.w-12.h-1');
        
        for (let i = 0; i < step - 1; i++) {
          if (steps[i]) {
            document.getElementById(steps[i]).classList.remove('bg-gray-200', 'text-gray-500');
            document.getElementById(steps[i]).classList.add('bg-green-600', 'text-white');
          }
          if (lines[i + 1]) {
            lines[i + 1].classList.remove('bg-gray-200');
            lines[i + 1].classList.add('bg-green-600');
          }
        }
      }

      // Back button handlers
      let currentStepNum = 1;
      
      // Update back link based on current step
      function updateBackLink() {
        const backLink = document.getElementById('back-link');
        if (currentStepNum === 1) {
          backLink.href = '/mulai';
          backLink.textContent = '← Kembali ke Login';
        } else {
          backLink.href = '#';
          backLink.textContent = '← Kembali';
        }
      }
      
      // Back link click handler
      document.getElementById('back-link').addEventListener('click', (e) => {
        if (currentStepNum > 1) {
          e.preventDefault();
          if (currentStepNum === 2) {
            // Go back to step 1
            document.getElementById('form-step2').classList.add('hidden');
            document.getElementById('form-step1').classList.remove('hidden');
            currentStepNum = 1;
            // Reset progress bar to step 1
            document.getElementById('step2').classList.add('bg-gray-200', 'text-gray-500');
            document.getElementById('step2').classList.remove('bg-green-600', 'text-white');
            const firstLine = document.querySelector('.w-12.h-1');
            if (firstLine) {
              firstLine.classList.add('bg-gray-200');
              firstLine.classList.remove('bg-green-600');
            }
          } else if (currentStepNum === 3) {
            // Go back to step 2
            document.getElementById('form-step3').classList.add('hidden');
            document.getElementById('form-step2').classList.remove('hidden');
            currentStepNum = 2;
            // Reset progress bar to step 2
            document.getElementById('step3').classList.add('bg-gray-200', 'text-gray-500');
            document.getElementById('step3').classList.remove('bg-green-600', 'text-white');
            const secondLine = document.querySelectorAll('.w-12.h-1')[1];
            if (secondLine) {
              secondLine.classList.add('bg-gray-200');
              secondLine.classList.remove('bg-green-600');
            }
          }
          updateBackLink();
        }
      });

      document.getElementById('next-step2').addEventListener('click', () => {
        document.getElementById('form-step2').classList.add('hidden');
        document.getElementById('form-step3').classList.remove('hidden');
        currentStepNum = 3;
        updateProgressBar(3);
        updateBackLink();
      });

      // Step 3: Finish
      document.getElementById('finish').addEventListener('click', () => {
        const bio = document.getElementById('bio').value;
        
        // Create complete user profile
        const member = {
          ...userData,
          level: selectedLevel,
          interests: selectedInterests,
          bio: bio || `${selectedLevel} yang tertarik dengan ${selectedInterests.join(', ')}`,
          username: userData.name?.toLowerCase().replace(/\s+/g, '_') || 'member',
          created_at: new Date().toISOString()
        };
        
        // Save to localStorage
        localStorage.setItem('kf13-member', JSON.stringify(member));
        sessionStorage.removeItem('kf13-onboarding');
        
        // Redirect to platform
        window.location.href = '/platform/overview';
      });

      // Redirect if no onboarding data - DISABLED FOR TESTING
      // if (!userData.name) {
      //   window.location.href = '/mulai';
      // }
    </script>
</AuthLayout>
